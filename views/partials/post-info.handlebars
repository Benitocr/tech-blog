    <article class="card">
        <div class = "card-header" >
            {{!-- {{#if loggedIn}} href = "" {{/if}} --}}
            <h2>{{title}}</h2>
            <span> Posted by {{user.username}} on {{format_date createdAt}}</span>
        </div>
        {{#if loggedIn}}
            <div class="card-body">
               <h1>Ejemplo</h1>
            </div>
        {{else}}
            <div class="card-body">
                {{post_comment}}
            </div>
        {{/if}}
    </article>   